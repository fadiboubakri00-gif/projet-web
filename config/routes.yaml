# config/routes.yaml

# Login page is now the root
app_login:
    path: /
    controller: App\Controller\AdminSecurityController::login

app_logout:
    path: /logout
    controller: App\Controller\AdminSecurityController::logout

# Controllers loaded via PHP attributes (for dashboard and other entities)
controllers:
    resource: ../src/Controller/
    type: attribute
