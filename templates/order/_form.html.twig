{{ form_start(form) }}
<div class="mb-4">
    {{ form_label(form.orderUser, 'User', {'label_attr': {'class': 'form-label fw-bold'}}) }}
    {{ form_widget(form.orderUser, {'attr': {'class': 'form-control'}}) }}
    {{ form_errors(form.orderUser) }}
</div>

<div class="mb-4">
    {{ form_label(form.products, 'Products', {'label_attr': {'class': 'form-label fw-bold'}}) }}
    {{ form_widget(form.products, {'attr': {'class': 'form-control'}}) }}
    {{ form_errors(form.products) }}
</div>

<div class="text-center">
    <button class="btn btn-primary btn-lg">
        <i class="bi bi-check-circle"></i> {{ button_label|default('Update') }}
    </button>
</div>
{{ form_end(form) }}